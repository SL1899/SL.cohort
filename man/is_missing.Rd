% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_missing.R
\name{is_missing}
\alias{is_missing}
\title{判断变量中的缺失值，包括N}
\usage{
is_missing(x)
}
\arguments{
\item{x}{需要判断是否缺失的向量。}
}
\value{
与 \code{x} 等长的逻辑向量。缺失值返回 \code{TRUE}，其他值返回 \code{FALSE}。
}
\description{
统一识别R原生缺失值以及数据中常见的字符型缺失值编码。
字符匹配不区分大小写，并自动去除字符串首尾的空格；
字符串中间的空格不会被去除。
}
\details{
该函数将以下情况判断为缺失：
\itemize{
\item R原生缺失值：\code{NA}，包括数值型 \code{NaN}；
\item 空字符串或仅包含空格的字符串；
\item \code{"N"}；
\item \code{"NA"}；
\item \code{"NULL"}；
\item \code{"N/A"}；
\item \code{"NaN"}；
\item \code{"."}；
\item \code{"*"}；
\item \code{"/"}。
}

字符型缺失编码不区分大小写，例如 \code{"n"}、\code{"na"}、\code{"null"}
也会被识别为缺失。函数仅去除字符串首尾的空格，例如
\code{" NA "}、\code{" N "} 和 \code{". "} 均会被识别为缺失；
字符串中间的空格不会被去除，例如 \code{"N A"} 不会被识别为 \code{"NA"}。
}
\examples{
x <- c(NA, "", " ", "N", "NA", "NULL", "N/A", "NaN", ".", "*", "/", "Yes")
is_missing(x)

}
