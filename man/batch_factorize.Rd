% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_factorize.R
\name{batch_factorize}
\alias{batch_factorize}
\title{批量设置变量的因子水平}
\usage{
batch_factorize(data, var_list, level_list, ordered = FALSE)
}
\arguments{
\item{data}{需要处理的数据框。}

\item{var_list}{需要转换为因子的变量名字符向量。}

\item{level_list}{命名列表，名称为变量名，每个元素为对应变量的因子水平。}

\item{ordered}{是否将转换后的变量设置为有序因子，默认为 \code{FALSE}。}
}
\value{
返回完成因子化后的数据框。
}
\description{
根据变量名称与命名的水平列表，批量将指定变量转换为因子，并按照给定顺序
设置因子水平。\code{level_list} 中每个变量的第一个水平通常作为默认参考水平。
}
\details{
\code{level_list} 必须为命名列表，并通过变量名称与 \code{var_list} 进行匹配，
因此二者的排列顺序不需要一致。例如：

\code{var_list = c("gender", "smoke")}

\code{level_list = list(smoke = c("never", "current", "quit"), gender = c("male", "female"))}

对每个变量，\code{level_list} 中给定水平的顺序即为因子水平顺序。
当 \code{ordered = FALSE} 时生成普通无序因子；当 \code{ordered = TRUE} 时生成有序因子。

函数在转换前会检查每个变量的全部非缺失原始取值是否均包含在对应的
\code{level_list} 中。如果存在未覆盖取值，将一次性列出所有存在问题的变量
及其未覆盖取值，并停止转换，避免这些取值被 \code{factor()} 静默转换为 \code{NA}。

原始 \code{NA} 不参与上述覆盖检查，并在因子化后继续保持为 \code{NA}。
\code{level_list} 中可以包含原始数据当前尚未出现的额外水平，也可以包含
\code{var_list} 中未指定转换的其他变量。
}
\examples{
data <- data.frame(
  gender = c("male", "female", "male"),
  smoke = c("never", "current", "quit")
)

var_list <- c("gender", "smoke")

level_list <- list(
  smoke = c("never", "current", "quit"),
  gender = c("male", "female")
)

data <- batch_factorize(
  data,
  var_list,
  level_list
)

}
